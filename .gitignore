# 비밀
.env

# 런타임 상태 — json 뿐 아니라 cron.log 도 여기 쌓인다
state/*
!state/.gitkeep

# 자료 캐시(저작물) + 다운로드 장부 — 전부 로컬 런타임 상태.
# meta.json 도 뺀다: 멱등에만 쓰는 장부고 같은 정보가 snapshot 에 있다.
# 추적하면 자료를 받을 때마다 워킹트리가 더러워져 미커밋이 쌓인다.
data/

# python
__pycache__/
*.pyc
.venv/
